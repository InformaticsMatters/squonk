---

- hosts: localhost
  vars_files:
  - ../../../env/{{ ansible_env.IM_PARAMETER_FILE }}

  tasks:
  - name: Run the Squonk 'Display Varibales' Tasks
    include_role:
      name: squonk-pipelines
      tasks_from: display-variables.yaml
