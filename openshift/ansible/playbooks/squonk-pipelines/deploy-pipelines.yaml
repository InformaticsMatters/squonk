---

# Deploy the Squonk 'Pipelines' to the Squonk application.
# Here we run the 'generic' pipelines role
# 'tuning' it with variables to run it for 'pipelines'

- hosts: localhost
  vars_files:
  - ../../../env/{{ ansible_env.IM_PARAMETER_FILE }}

  roles:
  - role: squonk-pipelines
    vars:
      origin: pipelines
